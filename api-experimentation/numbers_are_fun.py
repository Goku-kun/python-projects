import json
import requests

# number = input('Enter a number: ')
url = 'http://numbersapi.com/2020'

# while True:
#     if(requests.get(url).text != "42 is the number of kilometers in a marathon." and ):
# for i in range(10):
print(requests.get(url).text)
